CREATE TABLE notes (
    id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    noteType TEXT NOT NULL,
    contents TEXT NOT NULL,
    folder INTEGER REFERENCES traits(id) ON DELETE CASCADE NOT NULL
);